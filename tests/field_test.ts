// import { Field } from '../src/field';
// import * as chai from 'chai';
// import * as sinon from 'sinon';
//
// const sinonChai = require('sinon-chai');
// const expect = chai.expect;
// chai.use(sinonChai);
//
// describe('Field', () => {
//   describe('`containsCircle`', () => {
//     it('returns false if (`x` - `diameter`/ 2) < `field.x`', () => {
//       const field = new Field(0, 0, 1, 1);
//       expect(field.containsCircle(0, 0, 0.75)).toEqual(false);
//     });
//
//     it('returns false if (`x` - `diameter`/ 2) > (`field.x` + `field.xlength`)',
//       () => {
//         const field = new Field(0, 0, 1, 1);
//         expect(field.containsCircle(0.75, 0, 0.75)).toEqual(false);
//       });
//
//     it('returns true if x is within the boundary'), () => {
//     });
//
//     it('returns false if (`y` - `diameter`/ 2) < `field.y`', () => {
//       const field = new Field(0, 0, 1, 1);
//     });
//
//     it('returns false if (`y` - `diameter`/ 2) > (`field.y` + `field.ylength`)',
//       () => {
//       });
//
//     it('returns true if y is within the boundary'), () => {
//     });
//   });
// });

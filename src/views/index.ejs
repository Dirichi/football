<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% if (errors.length > 0) { %>
      <% errors.forEach((error) => { %>
        <div><%= error %></div>
      <% }); %>
    <% } %>
    <div class="">
      Instructions:
      <br>Use direction keys to move.
      <br>A to shoot.
      <br>S to pass.
      <br>D to request a pass. (only works on non-player characters)
      <br>F to chase after the ball.
    </div>
    <form action="/search" method="post">
      <div>Choose a position to play in.</div>
      <select name="preferredRoleType">
        <% roleTypes.forEach((value, representation) => { %>
          <option value=<%= value %>><%= representation %></option>
         <% }); %>
      </select>
      <input type="submit">
    </form>
  </body>
</html>
